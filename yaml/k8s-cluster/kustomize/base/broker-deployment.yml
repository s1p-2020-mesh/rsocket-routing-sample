apiVersion: apps/v1
kind: Deployment
metadata:
  name: broker
  labels:
    app: broker
    instance: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: broker
      instance: "1"
  strategy: {}
  template:
    metadata:
      labels:
        app: broker
        instance: "1"
    spec:
      containers:
      - name: broker
        image: marygabry1508/s1p-2020-mesh-rs-broker:0.0.1-SNAPSHOT
        envFrom:
          - configMapRef:
              name: broker-list
        resources: {}
        imagePullPolicy: Always
